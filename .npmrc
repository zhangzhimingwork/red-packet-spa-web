# 默认使用官方 npm registry（用于公共包）
registry=https://registry.npmjs.org/

# 私有包使用私有 registry（只针对 @my-ui scope）
@my-ui:registry=http://47.92.82.38:4873/

# 其他配置
engine-strict=true
save-exact=false
package-lock=true

# 强制使用 legacy peer deps 解决 React 19 兼容性问题
legacy-peer-deps=true

# 跳过可选依赖（puppeteer, cypress 等）
optional=false

# 禁用不必要的功能以加快安装速度
fund=false
audit=false
progress=false
