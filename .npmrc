# 公共包使用官方源
registry=https://registry.npmjs.org/

# 私有包使用你的私仓（scope registry）
@my-ui:registry=http://47.92.82.38:4873/
@yideng:registry=http://47.92.82.38:4873/

# 基本配置
engine-strict=true
save-exact=false
package-lock=true

# 强制使用 legacy peer deps
legacy-peer-deps=true

# 跳过可选依赖（减少内存占用）
optional=false

# 禁用不必要的功能
fund=false
audit=false
progress=false

# 允许 HTTP 连接（某些环境需要）
strict-ssl=false
