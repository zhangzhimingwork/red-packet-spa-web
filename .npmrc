# npm 配置
registry=https://registry.npmjs.org/

# 私有包使用私有 registry（只针对 @my-ui scope）
@my-ui:registry=http://47.92.82.38:4873/
engine-strict=true
save-exact=false
package-lock=true

# 强制使用 legacy peer deps 解决 React 19 兼容性问题
legacy-peer-deps=true

# 增加内存限制，防止 npm 安装时崩溃
# 注意：这个在 Cloudflare 环境可能不生效，但不会造成问题
node-options=--max-old-space-size=4096
